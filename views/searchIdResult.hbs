<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search ID Result</title>
    <style>
        table { border-collapse: collapse; width: 70%; }
        th, td { border: 1px solid #000; padding: 8px; text-align: left; }
        tr.highlight { background-color: #ffdddd; }
        img { max-width: 200px; margin: 5px; }
    </style>
</head>
<body>
    <h2>Property Details</h2>
    {{#highlightEmptyFee record["service fee"]}}
    <table>
        <tr class="highlight"><th>ID</th><td>{{record.id}}</td></tr>
        <tr class="highlight"><th>Name</th><td>{{record.NAME}}</td></tr>
        <tr class="highlight"><th>Host</th><td>{{record.[host name]}}</td></tr>
        <tr class="highlight"><th>Neighbourhood</th><td>{{record.neighbourhood}}</td></tr>
        <tr class="highlight"><th>Price</th><td>{{record.price}}</td></tr>
        <tr class="highlight"><th>Service Fee</th><td>{{serviceFee record["service fee"]}}</td></tr>
        <tr class="highlight"><th>Images</th>
            <td>
                {{#if record.thumbnail}}
                    <img src="{{record.thumbnail}}" alt="Thumbnail">
                {{/if}}
                {{#if record.images}}
                    {{#each record.images}}
                        <img src="{{this}}" alt="Image">
                    {{/each}}
                {{/if}}
            </td>
        </tr>
    </table>
    {{else}}
    <table>
        <tr><th>ID</th><td>{{record.id}}</td></tr>
        <tr><th>Name</th><td>{{record.NAME}}</td></tr>
        <tr><th>Host</th><td>{{record.[host name]}}</td></tr>
        <tr><th>Neighbourhood</th><td>{{record.neighbourhood}}</td></tr>
        <tr><th>Price</th><td>{{record.price}}</td></tr>
        <tr><th>Service Fee</th><td>{{serviceFee record["service fee"]}}</td></tr>
        <tr><th>Images</th>
            <td>
                {{#if record.thumbnail}}
                    <img src="{{record.thumbnail}}" alt="Thumbnail">
                {{/if}}
                {{#if record.images}}
                    {{#each record.images}}
                        <img src="{{this}}" alt="Image">
                    {{/each}}
                {{/if}}
            </td>
        </tr>
    </table>
    {{/highlightEmptyFee}}
</body>
</html>
